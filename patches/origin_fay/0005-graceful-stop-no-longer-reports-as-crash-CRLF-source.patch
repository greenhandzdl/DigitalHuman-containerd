diff -ruN a/main.py b/main.py
--- a/main.py
+++ b/main.py
@@ -154,8 +154,12 @@
     # 如果清理线程超过5秒还没完成，强制退出
     cleanup_thread.join(timeout=5.0)
     if cleanup_thread.is_alive():
+        # containerd 补丁：走到这里也照样是「被要求停止」（SIGTERM/SIGINT），不是启动失败。
+        # 原来的 os._exit(1) 让容器运行时把一次正常的 docker stop 记成崩溃退出码，
+        # 在 restart: on-failure 下还会把本该停掉的实例重新拉起来。拖住清理的只是
+        # 几个不肯退的非守护线程，而 os._exit 本来就是用来抛下它们的。
         util.log(1, '清理超时，立即强制退出...')
-        os._exit(1)
+        os._exit(0)
 
 # 注册退出处理和信号处理
 atexit.register(cleanup_on_exit)
diff -ruN a/scheduler/thread_manager.py b/scheduler/thread_manager.py
--- a/scheduler/thread_manager.py
+++ b/scheduler/thread_manager.py
@@ -1,5 +1,6 @@
 import ctypes
 import threading
+import time
 from threading import Thread
 
 
@@ -52,11 +53,16 @@
         except Exception as e:
             print(f"停止线程异常: {e}")
     
-    # 等待线程结束，但设置超时避免无限等待
-    import time
+    # 等待线程结束，但设置超时避免无限等待。
+    # containerd 补丁：这些 join 原本各排各的 2 秒，总耗时随线程数线性增长 ——
+    # 两个卡在 C 层 recv 上的 __connect 线程就要 4 秒，把 main.py 里 5 秒的清理
+    # 预算吃光，于是一次正常的 docker stop 被 os._exit(1) 记成崩溃退出。
+    # 改成共享一个截止时刻：raise_exception 已经全部发过了，剩下的只是等待，
+    # 多等第 N 个线程没有意义，超时的照样会被随后的 os._exit 抛下。
+    deadline = time.monotonic() + 2.0
     for thread in stopped_threads:
         try:
-            thread.join(timeout=2.0)  # 最多等待2秒
+            thread.join(timeout=max(0.0, deadline - time.monotonic()))
             if thread.is_alive():
                 print(f"线程 {thread.name} 超时未结束")
         except Exception as e:
